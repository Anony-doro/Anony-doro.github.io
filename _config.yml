# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Andoro's Space
email: baic0003@e.ntu.edu.sg
description: "Mechanical & Robotics Engineer | NTU Singapore"
# url: "https://yourusername.github.io" # 본인 주소로 변경

# Author Info
#twitter_username: username
github_username: Anony-doro
minimal_mistakes_skin: dark
email: "baic0003@e.ntu.edu.sg"
search: true

# Search Settings
search: true                   # 검색 기능 켜기
search_full_content: true      # 제목뿐만 아니라 본문 내용까지 검색
search_provider: lunr          # 내장 엔진 사용 (별도 API 키 필요 없음)
lunr:
  search_within_pages: true    # 페이지 내용도 검색 포함
# Build settings
markdown: kramdown
remote_theme: mmistakes/minimal-mistakes
# Outputting
permalink: /:categories/:title/
paginate: 5 # amount of posts to show
paginate_path: /page:num/
timezone: # https://en.wikipedia.org/wiki/List_of_tz_database_time_zones

include:
  - _pages

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

# Plugins (previously gems:)
plugins:
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-gist
  - jekyll-feed
  - jemoji
  - jekyll-include-cache

author:
  name   : "Geunmyeong Baick"
  avatar : "/assets/images/bio-photo.jpg"
  bio    : "NTU Mechanical Engineering Graduate (Highest Distinction) in Robotics and Mechatronics Stream"
  links:
    
    - label: "GitHub"
      icon: "fab fa-fw fa-github"
      url: "https://github.com/"

# _config.yml 파일 하단에 추가 또는 수정

footer:
  links:
    - label: "Email"
      icon: "fas fa-fw fa-envelope-square"
      url: "mailto:baic0003@e.ntu.edu.sg"
    - label: "LinkedIn"
      icon: "fab fa-fw fa-linkedin"
      url: "https://linkedin.com/in/본인아이디"
    - label: "GitHub"
      icon: "fab fa-fw fa-github"
      url: "https://github.com/Anony-doro"
    # - label: "Instagram"  # 필요하면 주석 풀고 쓰세요
    #   icon: "fab fa-fw fa-instagram"
    #   url: "https://instagram.com/..."
    

defaults:
  # _posts
  - scope:
      path: ""
      type: posts
    values:
      layout: single
      author_profile: true
      read_time: true
      comments: true
      share: true
      related: true
  # _pages
  - scope:
      path: "_pages"
      type: pages
    values:
      layout: single
      author_profile: true

category_archive:
  type: liquid
  path: /categories/
tag_archive:
  type: liquid
  path: /tags/
